ext {
  VERSION_CODE = 115
  VERSION_NAME_SUFFIX =
      System.getenv("BUILD_NAME_SUFFIX") != null ? System.getenv("BUILD_NAME_SUFFIX") : ""
  VERSION_NAME = "0.6.7.5" + VERSION_NAME_SUFFIX
  PUBLISH_VERSION = VERSION_NAME
  PUBLISH_GROUP_ID = "io.catappult"
}