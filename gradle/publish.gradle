ext {
    VERSION_CODE = 133
    VERSION_NAME_SUFFIX = System.getenv("BUILD_NAME_SUFFIX") != null ? System.getenv("BUILD_NAME_SUFFIX") : ""
    VERSION_NAME = "0.9.0.2" + VERSION_NAME_SUFFIX
    PUBLISH_VERSION = VERSION_NAME
    PUBLISH_GROUP_ID = "io.catappult"
}